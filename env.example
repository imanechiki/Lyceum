### Core (required)
# Used to sign cookies/JWTs/etc. Use any long random string in real projects.
PAYLOAD_SECRET=dev-secret-change-me

# MongoDB connection string
# - If you run MongoDB directly on your machine:
DATABASE_URI=mongodb://127.0.0.1:27017/lyceum
# - If you use Docker Compose (see README "Docker" section), use this instead:
# DATABASE_URI=mongodb://mongo:27017/lyceum

# Public URL for the site + Payload admin (used for CORS, previews, OG image host, etc.)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000

### Optional
# Used only if you enable scheduled jobs via a cron. Leave blank for local dev.
CRON_SECRET=

# Vercel Blob (uploads). Leave blank for local dev; uploads will fall back to local storage.
BLOB_READ_WRITE_TOKEN=

